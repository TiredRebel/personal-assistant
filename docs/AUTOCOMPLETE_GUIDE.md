# 📝 Автодоповнення команд (Tab Completion)

## Що це?

Personal Assistant підтримує автодоповнення команд за допомогою клавіші **Tab**.

## Як працює?

1. Запустіть програму: `uv run python -m personal_assistant.main`
2. Почніть вводити команду, наприклад: `add`
3. Натисніть **Tab** - команда автоматично доповниться або покаже варіанти

## Приклади

```
Enter command: add[Tab]
→ показує: add-contact  add-note

Enter command: add-c[Tab]
→ автоматично доповнює до: add-contact

Enter command: li[Tab]
→ показує: list-contacts  list-notes  list-tags
```

## Встановлення (Windows)

На Windows потрібна додаткова бібліотека `pyreadline3`:

```powershell
# Через UV
uv pip install pyreadline3

# Або через pip
pip install pyreadline3
```

## Встановлення (Linux/macOS)

На Linux та macOS модуль `readline` вже входить у стандартну бібліотеку Python.

## Доступні команди

Всі ці команди підтримують автодоповнення:

### Контакти
- `add-contact` - Додати контакт
- `search-contact` - Пошук контактів
- `list-contacts` - Список контактів
- `edit-contact` - Редагувати контакт
- `delete-contact` - Видалити контакт
- `birthdays` - Дні народження

### Нотатки
- `add-note` - Додати нотатку
- `search-note` - Пошук нотаток
- `list-notes` - Список нотаток
- `edit-note` - Редагувати нотатку
- `delete-note` - Видалити нотатку
- `search-by-tag` - Пошук за тегами
- `list-tags` - Список тегів

### Система
- `help` - Довідка
- `stats` - Статистика
- `clear` - Очистити екран
- `exit` - Вихід

## Альтернатива: Підказки команд

Якщо автодоповнення недоступне, програма автоматично пропонує схожі команди:

```
Enter command: hel
✗ Command not recognized: 'hel'

Did you mean:
  • help (help)
```

Це працює завжди, навіть без `readline`/`pyreadline3`.

## Fuzzy Matching

Програма також підтримує нечіткий пошук команд:

```
Enter command: fnd
→ розпізнає як: search-contact або search-note

Enter command: lst
→ розпізнає як: list-contacts або list-notes
```

## Природна мова

Можна вводити команди природною мовою:

```
Enter command: show all contacts
→ виконає: list-contacts

Enter command: find person
→ виконає: search-contact

Enter command: create note
→ виконає: add-note
```

## Тестування

Перевірте, чи працює автодоповнення:

```powershell
uv run python examples/test_autocomplete.py
```

Якщо бачите:
- `✓ readline доступний` - автодоповнення працює
- `✗ readline не доступний` - встановіть `pyreadline3` (Windows)

## Troubleshooting

### Проблема: Tab не працює

**Windows:**
```powershell
uv pip install pyreadline3
```

**Linux/macOS:**
Перевірте, чи встановлено `libreadline-dev`:
```bash
# Ubuntu/Debian
sudo apt-get install libreadline-dev

# macOS (через Homebrew)
brew install readline
```

### Проблема: Конфлікт з іншими програмами

Якщо Tab completion конфліктує з терміналом, використовуйте:
- Природну мову: `find contact John`
- Часткові команди: `lst` → розпізнається як `list-contacts`
- Підказки: програма сама запропонує схожі команди

## Дивіться також

- [CLI_MODULE.md](../docs/CLI_MODULE.md) - Повний опис CLI
- [QUICK_START.md](../QUICK_START.md) - Швидкий старт
